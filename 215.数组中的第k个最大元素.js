/*
 * @lc app=leetcode.cn id=215 lang=javascript
 *
 * [215] 数组中的第K个最大元素
 */
/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number}
 */
var findKthLargest = function(nums, k) {
    
};

/**
 * @param {number} k
 */
function heap(k) {
  this.heap = new Array(k);
}

heap.prototype.put = function() {
  
}
